# Dev profile: schema auto-update and optional local credentials.
# Activate with: SPRING_PROFILES_ACTIVE=dev or --spring.profiles.active=dev
# For production use default profile and set DB_PASSWORD, MAIL_PASSWORD via env.

spring.jpa.hibernate.ddl-auto=update

# Optional: local dev defaults (override with env in CI/production)
# spring.datasource.password=your_local_password
# spring.mail.username=your@email.com
# spring.mail.password=your_app_password
